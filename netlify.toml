[build]
publish = "out"  # The directory that contains the exported static files
command = "npm run build"  # No need for npm run export anymore

[dev]
command = "npm run dev"
port = 3000

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
